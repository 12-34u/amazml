# --- Data (1.9 GB raw, individual files exceed GitHub's 100 MB limit; download from the portal)
dataset/

# --- Generated artefacts (rebuilt by `python src/prepare.py` and later stages)
code/business_entity_resolution/artifacts/

# --- Submission outputs (regenerated by `python src/run_pipeline.py`; candidate_pairs.tsv can exceed 100 MB)
output/
*_submission.zip

# --- Python environment and caches
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# --- Editor / OS files (keep the shared workspace settings that open .ipynb as notebooks)
.vscode/*
!.vscode/settings.json
.DS_Store
.~lock.*#
